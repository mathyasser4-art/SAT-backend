## Changes Made

### 1. Added DISABLE_ADMIN_AUTH bypass to adminAuth
- **File:** `src/middleware/auth.js`
- **Change:** Added a bypass block at the top of `adminAuth`. When the Railway env variable `DISABLE_ADMIN_AUTH=true` is set, the middleware calls `next()` immediately (setting `req.userData = { role: 'admin' }`), skipping all JWT validation. Only `adminAuth` is affected — `userAuth`, `teacherAuth`, `studentAuth`, `schoolAuth`, `itAuth`, `supervisorAuth` are completely untouched.
- **Affected routes (all use adminAuth):** question, unit, chapter, subject, system, questionType
- **Student data routes remain protected** via their own separate middleware.
- **Revert:** Remove the 7-line bypass block (lines 42–48) from `adminAuth` in `src/middleware/auth.js`. Also remove `DISABLE_ADMIN_AUTH` from Railway environment variables.

### 2. Cleaned up leftover debug agent fetch calls from app.js
- **File:** `app.js`
- **Change:** Removed all `// #region agent log ... // #endregion` blocks containing `fetch('http://127.0.0.1:7242/ingest/...')` calls. These were added during a previous debug session and were making failed HTTP calls on every request in production.
- **Revert:** Restore the `fetch(...)` blocks from the previous version of `app.js` (see git history).

## Revert Status
- [ ] Change 1 - adminAuth bypass (remove bypass block + Railway env var)
- [ ] Change 2 - app.js debug fetch cleanup (restore from git history if needed)
